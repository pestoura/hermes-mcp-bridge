## Activation gates

The retry classifier is development-only until the client integration, fault
suite, isolated acceptance and single-slot acceptance are all green. No
production configuration is changed by this fragment.
