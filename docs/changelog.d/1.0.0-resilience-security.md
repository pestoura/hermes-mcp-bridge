## Security boundary

Unknown upstream operations fail closed and are never automatically retried.
Mutating operations remain non-retryable unless a future upstream idempotency
contract is explicitly documented and tested.
