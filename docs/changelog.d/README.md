# Changelog fragments

Temporary release-development fragments live in this directory while the
`1.0.0` work is split across independently reviewable pull requests. Before the
final release, accepted fragments are consolidated into `docs/changelog.md` and
this directory is removed or reset.
